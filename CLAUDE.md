# deep

## Agent skills

### Issue tracker

Issues live as GitHub issues in `s0h311/deep`, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, using their default label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context: a root `CONTEXT-MAP.md` points at one `CONTEXT.md` per package. See `docs/agents/domain.md`.
